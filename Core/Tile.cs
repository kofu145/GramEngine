namespace GramEngine.Core;

/// <summary>
/// Chose to utilize these as objects to make serialization easier - for the case of special attributes for
/// tiles, use your own separate map structures to handle that data, so that it doesn't have to overlay with the
/// existing tile system (will probably be faster too)
/// </summary>
public class Tile
{
    
}